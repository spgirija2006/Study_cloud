name: Deploy App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install dependencies
      run: echo "Installing..."

    - name: Build
      run: echo "Building..."

    - name: Deploy
      run: echo "Deploying..."
